--index-url https://mirrors.aliyun.com/pypi/simple/
#--index-url https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple

# 基础依赖
pytz>=2024.1
APScheduler>=3.10.4
dynaconf>=3.2.3
pyyaml>=6.0

# LLM 调用 (OpenAI 兼容接口)
openai>=1.12.0

# 微信自动化 (仅 Windows, 需微信 4.1.x 客户端)
# 安装命令: pip install pywechat127 --user --no-cache-dir
pywechat127>=1.9.8

# Web 服务 (如需后台管理)
uvicorn>=0.27.0

# 测试
pytest>=7.4.0
